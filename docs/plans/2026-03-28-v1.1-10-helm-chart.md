# v1.1 Helm Chart — Technická analýza

## Popis

Kubernetes Helm chart pro nasazení Shieldoo Gate. Umožní one-command deployment na Kubernetes s konfigurovatelným storage, databází, ingress a scaling.

### Proč

- Kubernetes je standard pro produkční deployment
- Docker Compose je vhodný pro dev/test, ne pro produkci
- `initial-analyse.md:920-929` definuje Helm deployment
- Enterprise zákazníci očekávají Helm chart

### Out of Scope

- Helm chart registry (publikace na charts.shieldoo.io) — budoucí
- Operator pattern — pouze Helm chart
- Auto-scaling (HPA) — manuální scaling v v1.1

## Aktuální stav

### Docker setup

`docker/Dockerfile`:
- Multi-stage build: Node (UI) → Go (binary) → Trivy → Alpine runtime
- Non-root user `sgw` (řádky 38-40)
- Porty: 5000 (PyPI), 4873 (npm), 5001 (NuGet), 5002 (Docker), 8080 (Admin)

`docker/docker-compose.yml`:
- Volumes: bridge-socket, gate-data, gate-cache, trivy-cache, config
- Scanner bridge sidecar
- Push registry sidecar (optional)

### Potřebné Kubernetes resources

| Resource | Účel |
|----------|------|
| Deployment | shieldoo-gate main container + scanner-bridge sidecar |
| Service | ClusterIP pro interní přístup |
| Ingress | External přístup (optional) |
| ConfigMap | config.yaml |
| Secret | OIDC secrets, DB credentials, webhook secrets |
| PVC | gate-data (SQLite), gate-cache (local cache) |
| ServiceAccount | pro cloud storage IAM (optional) |

## Návrh řešení

### Chart struktura

```
helm/shieldoo-gate/
├── Chart.yaml
├── values.yaml
├── templates/
│   ├── _helpers.tpl
│   ├── deployment.yaml
│   ├── service.yaml
│   ├── ingress.yaml
│   ├── configmap.yaml
│   ├── secret.yaml
│   ├── pvc.yaml              # pro SQLite + local cache
│   ├── serviceaccount.yaml
│   └── tests/
│       └── test-connection.yaml
└── .helmignore
```

### values.yaml (klíčové sekce)

```yaml
replicaCount: 1  # >1 vyžaduje PostgreSQL + cloud storage

image:
  repository: ghcr.io/cloudfieldcz/shieldoo-gate
  tag: ""  # defaults to chart appVersion
  pullPolicy: IfNotPresent

ports:
  pypi: 5000
  npm: 4873
  nuget: 5001
  docker: 5002
  admin: 8080
  # maven: 5003  (pokud v1.1 adaptéry enabled)

service:
  type: ClusterIP

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: shieldoo-gate.example.com
      paths:
        - path: /
          pathType: Prefix
          port: 8080  # admin

persistence:
  data:
    enabled: true
    size: 1Gi
    storageClass: ""
  cache:
    enabled: true
    size: 50Gi
    storageClass: ""

scannerBridge:
  enabled: true
  image:
    repository: ghcr.io/cloudfieldcz/shieldoo-scanner-bridge
    tag: ""

config: {}  # inline config.yaml overrides

existingSecret: ""  # name of pre-existing Secret

resources:
  limits:
    memory: 2Gi
  requests:
    cpu: 500m
    memory: 512Mi
```

### Sidecar pattern

Scanner bridge jako sidecar kontejner v Deployment (shared Unix socket via emptyDir volume):

```yaml
containers:
  - name: shieldoo-gate
    ...
    volumeMounts:
      - name: bridge-socket
        mountPath: /tmp
  - name: scanner-bridge
    image: "{{ .Values.scannerBridge.image.repository }}"
    volumeMounts:
      - name: bridge-socket
        mountPath: /tmp
volumes:
  - name: bridge-socket
    emptyDir: {}
```

## Dotčené soubory

### Nové soubory

- `helm/shieldoo-gate/Chart.yaml`
- `helm/shieldoo-gate/values.yaml`
- `helm/shieldoo-gate/templates/*.yaml` (8+ template souborů)
- `helm/shieldoo-gate/.helmignore`

### Upravené soubory

- `docs/deployment.md` — Helm deployment instrukce
- `docs/index.md` — link na Helm docs

### Soubory BEZ změn

- Vše v `internal/`, `cmd/`, `ui/` — Helm chart je čistě deployment

## Implementační fáze

### Fáze 1: Základní Helm chart

- [ ] Chart.yaml + values.yaml
- [ ] Deployment template (main + scanner-bridge sidecar)
- [ ] Service + ConfigMap + Secret templates
- [ ] PVC template (data + cache)
- [ ] Ingress template (optional)
- [ ] ServiceAccount template
- [ ] `helm lint` + `helm template` validace
- [ ] Test connection template
- [ ] Docs

## Testování

- `helm lint helm/shieldoo-gate/`
- `helm template shieldoo-gate helm/shieldoo-gate/ --debug`
- `helm install --dry-run` s různými values kombinacemi
- Deploy na kind/minikube cluster

## Reference

- `docs/initial-analyse.md:920-929` — Helm spec
- `docker/Dockerfile` — container setup
- `docker/docker-compose.yml` — reference pro volumes/ports
