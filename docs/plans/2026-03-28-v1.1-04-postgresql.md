# v1.1 PostgreSQL HA Mode — Technická analýza

## Popis

Přidání PostgreSQL jako alternativního databázového backendu vedle SQLite. Umožní horizontální škálování a vysokou dostupnost (HA) pro produkční nasazení.

### Proč

- SQLite je single-writer — při vysokém concurrency (50+ paralelních requestů) dochází k write lock contention
- SQLite nepodporuje replikaci — single point of failure
- Enterprise nasazení vyžadují HA databázi
- `initial-analyse.md:218,478-482` definuje PostgreSQL jako HA backend

### Out of Scope

- Migrace dat SQLite → PostgreSQL (manuální nástroj, budoucí)
- Connection pooling (PgBouncer) — dokumentace doporučení, ne implementace
- Read replicas — budoucí

## Aktuální stav

### Database inicializace

`internal/config/db.go:40-89`:
- `InitDB(dbPath string)` — hardcoded na SQLite
- Řádek 41: `sqlx.Open("sqlite3", dbPath)`
- Řádky 47-56: SQLite-specifické PRAGMAs (`journal_mode=WAL`, `foreign_keys=ON`, `busy_timeout=5000`)
- Řádky 74-86: Manuální migration runner (čte z `schema_migrations` tabulky)

### SQLite-specifický SQL v migraci

`internal/config/migrations/`:
- **`INTEGER PRIMARY KEY AUTOINCREMENT`** — 001 (řádky 15, 37), 002 (řádek 2), 003 (řádek 9), 004 (řádek 3)
  - PostgreSQL ekvivalent: `SERIAL PRIMARY KEY` nebo `GENERATED ALWAYS AS IDENTITY`
- **`INSERT OR REPLACE`** — `internal/adapter/base.go:152,170`
  - PostgreSQL ekvivalent: `INSERT ... ON CONFLICT ... DO UPDATE`
- **`ON CONFLICT(...) DO UPDATE`** — `internal/adapter/docker/tags.go:32-37`
  - Kompatibilní s PostgreSQL (od 9.5)
- **`datetime()` funkce** — v nové migraci z v1.1-02 (rescan bootstrap)
  - PostgreSQL ekvivalent: `NOW() + INTERVAL '24 hours'`

### Config

`internal/config/config.go:90-97`:
```go
type DatabaseConfig struct {
    Backend string       `mapstructure:"backend"`  // existuje, ale nepoužit
    SQLite  SQLiteConfig `mapstructure:"sqlite"`
}
```
`Backend` field existuje ale je ignorován — `main.go:60` hardcoded na SQLite.

### go.mod

- `github.com/mattn/go-sqlite3 v1.14.37` — přítomen
- `github.com/lib/pq` — NENÍ v go.mod (pouze transitive v go.sum)

| Aspekt | Současný stav | Navrhovaný stav |
|--------|--------------|-----------------|
| Backend výběr | Hardcoded SQLite | Config-driven (`sqlite` / `postgres`) |
| Driver | mattn/go-sqlite3 | + lib/pq (PostgreSQL) |
| Migrace | SQLite-only SQL | Dual-dialect migrace |
| PRAGMAs | SQLite-specifické | Podmíněné (pouze pro SQLite) |
| `INSERT OR REPLACE` | SQLite syntax | `ON CONFLICT ... DO UPDATE` (obojí) |

## Návrh řešení

### Architektura

```
config.yaml
  database:
    backend: "postgres"    ←── nebo "sqlite"
    postgres:
      dsn: "postgres://..."
    sqlite:
      path: "/var/lib/..."
         │
         ▼
config.InitDB(cfg.Database)
         │
    ┌────┴────┐
    │ sqlite  │ postgres │
    │ Open()  │ Open()   │
    │ PRAGMAs │ (none)   │
    │ migrate │ migrate  │
    └─────────┘──────────┘
         │
         ▼
    *sqlx.DB (same interface everywhere)
```

**Klíčová rozhodnutí:**

1. **`sqlx.DB` interface zůstává** — veškerý kód používá `*sqlx.DB`, žádné změny v adaptérech, API, scheduleru.
2. **Dual-dialect migrace** — každá migrace má SQLite a PostgreSQL verzi. Migration runner detekuje backend a spustí správnou variantu.
3. **`?` placeholder zůstává** — `sqlx` s `Rebind()` automaticky přepisuje `?` na `$1, $2, ...` pro PostgreSQL.
4. **`INSERT OR REPLACE` → `ON CONFLICT DO UPDATE`** — refaktor na ANSI kompatibilní syntax (funguje na obou).
5. **PRAGMAs podmíněné** — pouze pro SQLite backend.

### Změny v `db.go`

```go
// internal/config/db.go

func InitDB(cfg DatabaseConfig) (*sqlx.DB, error) {
    switch cfg.Backend {
    case "postgres":
        return initPostgres(cfg.Postgres)
    default:
        return initSQLite(cfg.SQLite)
    }
}

func initSQLite(cfg SQLiteConfig) (*sqlx.DB, error) {
    // existující logika — Open, PRAGMAs, migrate
}

func initPostgres(cfg PostgresConfig) (*sqlx.DB, error) {
    db, err := sqlx.Open("postgres", cfg.DSN)
    if err != nil {
        return nil, fmt.Errorf("config: connecting to postgres: %w", err)
    }
    db.SetMaxOpenConns(cfg.MaxOpenConns)    // default 25
    db.SetMaxIdleConns(cfg.MaxIdleConns)    // default 5
    db.SetConnMaxLifetime(cfg.ConnMaxLifetime) // default 5m
    if err := db.Ping(); err != nil {
        return nil, fmt.Errorf("config: pinging postgres: %w", err)
    }
    if err := runMigrations(db, "postgres"); err != nil {
        return nil, err
    }
    return db, nil
}
```

### Konfigurace

```go
type DatabaseConfig struct {
    Backend  string         `mapstructure:"backend"`   // "sqlite" | "postgres"
    SQLite   SQLiteConfig   `mapstructure:"sqlite"`
    Postgres PostgresConfig `mapstructure:"postgres"`
}

type PostgresConfig struct {
    DSN             string `mapstructure:"dsn"`               // connection string
    MaxOpenConns    int    `mapstructure:"max_open_conns"`    // default 25
    MaxIdleConns    int    `mapstructure:"max_idle_conns"`    // default 5
    ConnMaxLifetime string `mapstructure:"conn_max_lifetime"` // default "5m"
}
```

YAML:
```yaml
database:
  backend: "postgres"
  postgres:
    dsn: "postgres://shieldoo:password@localhost:5432/shieldoo_gate?sslmode=require"
    max_open_conns: 25
    max_idle_conns: 5
    conn_max_lifetime: "5m"
```

**Bezpečnost:** DSN obsahuje credentials. Podporovat env var override přes viper: `SGW_DATABASE_POSTGRES_DSN`.

### Dual-dialect migrace

```
internal/config/migrations/
├── sqlite/
│   ├── 001_init.sql
│   ├── 002_policy_overrides.sql
│   ├── 003_docker_registry.sql
│   ├── 004_docker_tags.sql
│   └── ... (budoucí)
└── postgres/
    ├── 001_init.sql
    ├── 002_policy_overrides.sql
    ├── 003_docker_registry.sql
    ├── 004_docker_tags.sql
    └── ... (budoucí)
```

Rozdíly:
- `INTEGER PRIMARY KEY AUTOINCREMENT` → `SERIAL PRIMARY KEY`
- `datetime('now', '+24 hours')` → `NOW() + INTERVAL '24 hours'`
- `PRAGMA` příkazy → odstraněny

### SQL refaktoring — `INSERT OR REPLACE`

`internal/adapter/base.go:152,170` — `INSERT OR REPLACE INTO artifacts` / `artifact_status`:

Refaktor na:
```sql
INSERT INTO artifacts (...) VALUES (...)
ON CONFLICT (id) DO UPDATE SET
    upstream_url = EXCLUDED.upstream_url,
    sha256 = EXCLUDED.sha256,
    ...
```

Funguje na SQLite 3.24+ (2018) i PostgreSQL 9.5+ (2016). Zpětně kompatibilní.

### Placeholder rebinding

`sqlx` podporuje `Rebind()` pro přepis `?` na `$1`. Buď:
- (a) Volat `db.Rebind(query)` před každým Exec/Query
- (b) Použít `sqlx.Named()` s named parameters

**Doporučení:** (a) je jednodušší. Přidat helper:

```go
// internal/config/db.go
func (db *DB) Exec(query string, args ...any) (sql.Result, error) {
    return db.DB.Exec(db.Rebind(query), args...)
}
```

Alternativa: Nechat `?` a nastavit `sqlx.BindType` na init. `sqlx` to řeší interně pokud se používá `sqlx.DB.Exec`/`Get`/`Select` (ne `database/sql` raw).

## Dotčené soubory

### Nové soubory

- `internal/config/migrations/postgres/` — PostgreSQL verze všech migrací (001-004+)
- `internal/config/migrations/sqlite/` — Přesun existujících migrací

### Upravené soubory

- `internal/config/db.go` — `InitDB(cfg DatabaseConfig)`, `initPostgres()`, dual-dialect migration runner
- `internal/config/config.go:90-97` — přidat `PostgresConfig` struct, rozšířit `DatabaseConfig`
- `internal/adapter/base.go:152,170` — `INSERT OR REPLACE` → `ON CONFLICT DO UPDATE`
- `cmd/shieldoo-gate/main.go:60` — `config.InitDB(cfg.Database)` místo `config.InitDB(cfg.Database.SQLite.Path)`
- `go.mod` — přidat `github.com/lib/pq`
- `docker/config.yaml` — přidat `postgres:` sekci (disabled)
- `config.example.yaml` — přidat `postgres:` sekci
- `docs/configuration.md` — dokumentace PostgreSQL setup

### Soubory BEZ změn

- `internal/adapter/pypi/pypi.go` — používá `*sqlx.DB`, kompatibilní
- `internal/adapter/npm/npm.go` — stejné
- `internal/adapter/nuget/nuget.go` — stejné
- `internal/adapter/docker/` — stejné
- `internal/api/` — stejné
- `internal/policy/` — `?` placeholders, `sqlx` rebind řeší
- `internal/scanner/` — nepoužívá DB přímo

## Akceptační kritéria

1. `database.backend: "postgres"` → aplikace se připojí k PostgreSQL a migruje schema.
2. `database.backend: "sqlite"` → chování identické jako dnes.
3. Všechny existující testy projdou s oběma backendy.
4. Žádný `INSERT OR REPLACE` v codebase — nahrazeno `ON CONFLICT DO UPDATE`.
5. PostgreSQL migrace vytvoří identické schéma jako SQLite (modulo syntax).
6. Connection pooling konfigurovatelný (max_open_conns, max_idle_conns, conn_max_lifetime).

## Implementační fáze

### Fáze 1: PostgreSQL driver + dual migrations

- [ ] Přidat `github.com/lib/pq` do go.mod
- [ ] Přesunout stávající migrace do `migrations/sqlite/`
- [ ] Vytvořit `migrations/postgres/` s PostgreSQL verzí
- [ ] `initPostgres()` v db.go s connection pooling
- [ ] Dual-dialect migration runner
- [ ] `PostgresConfig` struct + config validace
- [ ] Změna `main.go` pro `InitDB(cfg.Database)`

### Fáze 2: SQL kompatibilita

- [ ] Refaktor `INSERT OR REPLACE` → `ON CONFLICT DO UPDATE` (base.go)
- [ ] Ověřit všechny SQL dotazy s PostgreSQL (placeholder `?` → `sqlx.Rebind`)
- [ ] Integration testy s real PostgreSQL (docker-compose service)
- [ ] Config + docs

## Rizika a mitigace

| Riziko | Dopad | Pravděpodobnost | Mitigace |
|--------|-------|-----------------|----------|
| Skrytý SQLite-only SQL | Vysoký — runtime error na PostgreSQL | Střední | Integration test suite s PostgreSQL |
| `?` vs `$1` placeholder | Vysoký — broken queries | Střední | sqlx.Rebind(), CI test s oběma backendy |
| Migration divergence | Střední — schema drift | Nízká | CI check: obě migrace musí vytvořit kompatibilní schéma |
| DSN s credentials v config | Střední — security | Nízká | Env var override SGW_DATABASE_POSTGRES_DSN |

## Testování

### Unit testy

- `TestInitDB_SQLiteBackend` — SQLite inicializace funguje jako dříve
- `TestInitDB_PostgresBackend` — PostgreSQL inicializace s mock/real PG
- `TestMigrations_PostgresSchema` — migrace vytvořené identické tabulky
- `TestInsertArtifact_PostgreSQL` — ON CONFLICT DO UPDATE funguje
- `TestInsertArtifact_SQLite` — ON CONFLICT DO UPDATE funguje na SQLite

### Integrační testy

- docker-compose s PostgreSQL 15+ service
- Spustit celou test suite s `SGW_DATABASE_BACKEND=postgres`

### Verifikace

```bash
# Ověřit žádný INSERT OR REPLACE
grep -rn "INSERT OR REPLACE" internal/ | grep -v "_test.go"

# Ověřit lib/pq import
grep "lib/pq" go.mod
```

## Poznámky

- **ANSI SQL only:** Dle CLAUDE.md — žádné vendor extensions. `ON CONFLICT DO UPDATE` je podporovaný oběma databázemi.
- **CGO závislost:** SQLite vyžaduje CGO (`mattn/go-sqlite3`). PostgreSQL (`lib/pq`) je pure Go. Pro PostgreSQL-only nasazení by šlo odstranit CGO dependency — ale to je budoucí optimalizace.
- **Zpětná kompatibilita:** 100% — default backend zůstává `sqlite`.

## Reference

- `docs/initial-analyse.md:218,478-482` — PostgreSQL HA design
- `internal/config/db.go:40-89` — současná SQLite inicializace
- `internal/config/config.go:90-97` — DatabaseConfig struct
- `internal/adapter/base.go:150-188` — INSERT OR REPLACE usage
- `internal/config/migrations/` — současné migrace
