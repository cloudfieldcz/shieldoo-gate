# v1.1 Go Modules Adapter — Technická analýza

## Popis

Nový protokolový adaptér pro Go module proxy (GOPROXY protocol). Implementuje Go module proxy API jako transparent proxy s artifact scanning.

### Proč

- Go ekosystém roste — Go modules je standard od Go 1.13
- Supply chain rizika: typosquatting, dependency confusion na pkg.go.dev
- Shieldoo Gate je sám napsaný v Go — dogfooding use case
- `initial-analyse.md` implicitly lists Go modules among v1.1 adapters

### Out of Scope

- Module publishing — pouze proxy/read
- Checksum database validation (sum.golang.org) — budoucí

## Návrh řešení

### Go Module Proxy Protocol (GOPROXY)

```
GET /{module}/@v/list           ← version listing (text, one per line)
GET /{module}/@v/{version}.info ← version metadata (JSON: {"Version": "v1.0.0", "Time": "..."})
GET /{module}/@v/{version}.mod  ← go.mod file (text)
GET /{module}/@v/{version}.zip  ← source zip (trigger scan)
GET /{module}/@latest           ← latest version info
```

Module path encoding: `github.com/Foo/Bar` → `github.com/!foo/!bar` (uppercase → `!` + lowercase)

### Routing

```go
r.Get("/{module}/@v/list", a.handleVersionList)
r.Get("/{module}/@v/{version}.info", a.handleVersionInfo)
r.Get("/{module}/@v/{version}.mod", a.handleGoMod)      // pass-through
r.Get("/{module}/@v/{version}.zip", a.handleZipDownload) // trigger scan
r.Get("/{module}/@latest", a.handleLatest)
```

Wildcard `{module}` zachycuje celou module path (včetně `/`). Chi `**` nebo custom routing.

### Artifact ID format

`go:github.com/user/repo:v1.2.3`

### Nový balík

```
internal/adapter/gomod/
├── gomod.go       # GoModAdapter
└── gomod_test.go  # Unit testy
```

### Scanner support

- `EcosystemGo` — nová konstanta v `internal/scanner/interface.go` (přidat)
- Trivy: podporuje Go (go.mod scan)
- OSV: podporuje Go
- Built-in: generic scanners

### Konfigurace

```yaml
ports:
  gomod: 5005
upstreams:
  gomod: "https://proxy.golang.org"
```

Klient konfigurace:
```bash
export GOPROXY=http://shieldoo-gate:5005
```

## Dotčené soubory

### Nové soubory

- `internal/adapter/gomod/gomod.go` + `gomod_test.go`

### Upravené soubory

- `internal/scanner/interface.go` — přidat `EcosystemGo`
- `internal/config/config.go` — GoMod port + upstream
- `cmd/shieldoo-gate/main.go` — GoMod adapter init
- Config + docs

## Implementační fáze

### Fáze 1: Go Modules Adapter (jediná fáze)

- [ ] `GoModAdapter` struct + router
- [ ] Module path encoding/decoding
- [ ] Zip download → scan → serve pipeline
- [ ] Metadata/list/mod pass-through
- [ ] `EcosystemGo` konstanta
- [ ] Config + main.go integrace
- [ ] Unit testy
- [ ] Docs

## Testování

- `TestGoModAdapter_DownloadZip_ScanAndServe`
- `TestGoModAdapter_ModulePath_Encoding` — uppercase encoding
- `TestGoModAdapter_VersionList_PassThrough`
- `TestGoModAdapter_CacheHit_ServesFromCache`

## Reference

- [Go Module Proxy Protocol](https://go.dev/ref/mod#goproxy-protocol)
- `internal/adapter/pypi/pypi.go` — reference adapter pattern
- `internal/scanner/interface.go` — Ecosystem constants
