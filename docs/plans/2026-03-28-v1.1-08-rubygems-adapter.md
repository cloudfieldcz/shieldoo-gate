# v1.1 RubyGems Adapter — Technická analýza

## Popis

Nový protokolový adaptér pro RubyGems (Ruby package manager). Implementuje RubyGems API jako transparent proxy s artifact scanning.

### Proč

- Ruby ekosystém — Rails je stále masivně používaný framework
- Supply chain útoky na RubyGems: typosquatting, backdoored gems
- `initial-analyse.md:596-599` definuje RubyGems adapter jako v1.1

### Out of Scope

- Gem push (upload) — pouze proxy/read
- Bundler API v2 (compact index) — pouze v1 API

## Návrh řešení

### RubyGems API Layout

```
GET /api/v1/gems/{name}.json                    ← gem metadata (JSON)
GET /api/v1/versions/{name}.json                ← version listing
GET /gems/{name}-{version}.gem                  ← download gem (trigger scan)
GET /quick/Marshal.4.8/{name}-{version}.gemspec.rz  ← compressed gemspec
GET /specs.4.8.gz                               ← full index (proxy pass-through)
GET /latest_specs.4.8.gz                        ← latest index
GET /prerelease_specs.4.8.gz                    ← prerelease index
```

### Routing

```go
r.Get("/api/v1/gems/{name}.json", a.handleGemInfo)
r.Get("/api/v1/versions/{name}.json", a.handleVersions)
r.Get("/gems/{name}-{version}.gem", a.handleGemDownload) // trigger scan
r.Get("/quick/Marshal.4.8/*", a.handleQuickSpec)          // pass-through
r.Get("/specs.4.8.gz", a.handleIndex)                     // pass-through
r.Get("/latest_specs.4.8.gz", a.handleIndex)              // pass-through
r.Get("/prerelease_specs.4.8.gz", a.handleIndex)          // pass-through
```

### Artifact ID format

`rubygems:rails:7.1.3`

### Nový balík

```
internal/adapter/rubygems/
├── rubygems.go       # RubyGemsAdapter
└── rubygems_test.go  # Unit testy
```

### Scanner support

- `EcosystemRubyGems` — nová konstanta v `internal/scanner/interface.go` (přidat)
- Trivy: podporuje Ruby (Gemfile.lock scan)
- OSV: podporuje RubyGems
- GuardDog: nepodporuje RubyGems
- Built-in: generic scanners (hash, obfuscation, exfil)

### Konfigurace

```yaml
ports:
  rubygems: 5004
upstreams:
  rubygems: "https://rubygems.org"
```

## Dotčené soubory

### Nové soubory

- `internal/adapter/rubygems/rubygems.go` + `rubygems_test.go`

### Upravené soubory

- `internal/scanner/interface.go` — přidat `EcosystemRubyGems`
- `internal/config/config.go` — RubyGems port + upstream
- `cmd/shieldoo-gate/main.go` — RubyGems adapter init
- Config + docs

## Implementační fáze

### Fáze 1: RubyGems Adapter (jediná fáze)

- [ ] `RubyGemsAdapter` struct + router
- [ ] Gem download → scan → serve pipeline
- [ ] Metadata/index pass-through
- [ ] `EcosystemRubyGems` konstanta
- [ ] Config + main.go integrace
- [ ] Unit testy
- [ ] Docs

## Testování

- `TestRubyGemsAdapter_DownloadGem_ScanAndServe`
- `TestRubyGemsAdapter_MetadataJSON_PassThrough`
- `TestRubyGemsAdapter_CacheHit_ServesFromCache`
- `TestRubyGemsAdapter_MaliciousGem_Returns403`

## Reference

- `docs/initial-analyse.md:596-599` — RubyGems adapter spec
- [RubyGems API](https://guides.rubygems.org/rubygems-org-api/)
- `internal/adapter/pypi/pypi.go` — reference adapter pattern
