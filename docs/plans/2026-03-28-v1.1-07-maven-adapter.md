# v1.1 Maven Adapter — Technická analýza

## Popis

Nový protokolový adaptér pro Maven/Gradle repozitáře. Implementuje Maven repository HTTP layout jako transparent proxy s artifact scanning.

### Proč

- Java/Kotlin ekosystém je masivní — Maven Central má 12M+ artefaktů
- Supply chain útoky na Maven: dependency confusion, typosquatting (např. "log4j" typos)
- `initial-analyse.md:591-594` definuje Maven adapter jako v1.1
- Gradle (nejpopulárnější Java build tool) používá Maven repository formát

### Out of Scope

- Gradle-specific protokol (Gradle Module Metadata) — pouze Maven repository layout
- Maven deploy (upload) — pouze proxy/read

## Aktuální stav

### Adapter pattern

Všechny existující adaptéry (PyPI, npm, NuGet) sdílejí identický pattern:
1. Constructor: `New*Adapter(db, cacheStore, scanEngine, policyEngine, upstreamURL)`
2. Chi router s ecosystem-specific routes
3. `downloadScanServe()` pipeline: download → scan → policy → persist → serve/block
4. Cache hit path: check status → serve if CLEAN
5. Artifact ID: `"{ecosystem}:{name}:{version}"`

### Ecosystem constants

`internal/scanner/interface.go:10-15` — **`EcosystemMaven` NEEXISTUJE!** Definovány jsou pouze: `EcosystemPyPI`, `EcosystemNPM`, `EcosystemDocker`, `EcosystemNuGet`. Nutné přidat `EcosystemMaven Ecosystem = "maven"`.

### Scanner support

- GuardDog: nepodporuje Maven (pouze PyPI, npm)
- Trivy: podporuje Maven (filesystem scan)
- OSV: podporuje Maven (API lookup)
- Built-in scanners: většina generic (hash verifier, obfuscation, exfil)

| Aspekt | Současný stav | Navrhovaný stav |
|--------|--------------|-----------------|
| Maven proxy | Neexistuje | Nový adaptér |
| Port | N/A | Nový port (config) |
| Upstream | N/A | Maven Central (repo1.maven.org) |

## Návrh řešení

### Maven Repository HTTP Layout

```
GET /{group}/{artifact}/{version}/{artifact}-{version}.jar       ← download JAR
GET /{group}/{artifact}/{version}/{artifact}-{version}.pom       ← POM metadata
GET /{group}/{artifact}/{version}/{artifact}-{version}.jar.sha1  ← checksum
GET /{group}/{artifact}/maven-metadata.xml                        ← version listing
```

Group ID je path-encoded: `org.apache.commons` → `org/apache/commons`

**Classifier support:** Maven artefakty mohou mít classifiers (např. `artifact-1.0-sources.jar`, `artifact-1.0-javadoc.jar`). Path parser musí správně rozlišit classifier od verze.

**Signature files:** Maven Central servíruje `.asc` (GPG signatures). Přidat `.asc` do pass-through seznamu vedle `.sha1`, `.md5`, `.sha256`.

### Routing

```go
r.Get("/*", a.handleRequest)  // catch-all, parse path to determine action
```

Maven layout je path-based, ne route-based. Handler parsuje cestu:
- `*.jar` / `*.war` / `*.aar` → artifact download (trigger scan)
- `*.pom` → POM metadata (proxy pass-through, no scan)
- `*.sha1` / `*.md5` / `*.sha256` → checksum (proxy pass-through)
- `*.asc` → GPG signature (proxy pass-through)
- `maven-metadata.xml` → version listing (proxy pass-through)

**Bezpečnost — path traversal ochrana:**
- `path.Clean()` na vstupní cestu
- Odmítnout cesty obsahující `..`
- Validovat každý path component: pouze alfanumerické, tečky, pomlčky
- Upstream URL construction: používat `url.JoinPath()`, NIKDY string concatenation

**POM/checksum pass-through — známé omezení:**
POM soubory obsahují `<repositories>` a `<build><plugins>` elementy, které mohou odkazovat na attacker-controlled servery. Aktuálně se POM soubory proxují bez inspekce. Dokumentovat jako known limitation. Budoucí fáze: POM XML validation (flag suspicious `<repository>` entries).

### Artifact ID format

`maven:org.apache.commons:commons-lang3:3.14.0`

Group ID + artifact ID + version, separátor `:`.

### Nový balík

```
internal/adapter/maven/
├── maven.go       # MavenAdapter implementace
└── maven_test.go  # Unit testy
```

### Konfigurace

```go
// Rozšíření PortsConfig
type PortsConfig struct {
    // ... existing ...
    Maven  int `mapstructure:"maven"`   // default 5003
}

// Rozšíření UpstreamsConfig
type UpstreamsConfig struct {
    // ... existing ...
    Maven  string `mapstructure:"maven"` // default "https://repo1.maven.org/maven2"
}
```

### Integrace v main.go

```go
mavenUpstream := fallback(cfg.Upstreams.Maven, "https://repo1.maven.org/maven2")
mavenAdapter := maven.NewMavenAdapter(db, cacheStore, scanEngine, policyEngine, mavenUpstream)

servers = append(servers, struct{...}{"maven", cfg.Ports.Maven, mavenAdapter})
```

## Dotčené soubory

### Nové soubory

- `internal/adapter/maven/maven.go` — MavenAdapter
- `internal/adapter/maven/maven_test.go` — Unit testy

### Upravené soubory

- `internal/config/config.go` — Maven port + upstream
- `cmd/shieldoo-gate/main.go` — Maven adapter init + HTTP server
- `docker/config.yaml`, `config.example.yaml` — maven port + upstream
- `docs/configuration.md`, `docs/adapters.md` — dokumentace
- `internal/scanner/interface.go:10-15` — přidat `EcosystemMaven Ecosystem = "maven"`

### Soubory BEZ změn

- Všechny ostatní adaptéry, cache, policy, API

## Akceptační kritéria

1. Maven `mvn dependency:resolve` přes proxy uspěje pro čistý artefakt.
2. Malicious JAR je blokován s HTTP 403.
3. POM a checksum requesty jsou proxovány bez scanningu.
4. `.asc` (GPG signature) requesty jsou proxovány bez scanningu.
5. Artifact s classifierem (e.g., `-sources.jar`) je správně parsován a scanován.
6. Path traversal pokus (e.g., `/../internal/`) vrátí HTTP 400.
7. Upstream URL je konstruovaná bezpečně (url.JoinPath, ne string concat).

## Implementační fáze

### Fáze 1: Maven Adapter (jediná fáze)

- [ ] `MavenAdapter` struct + constructor + router
- [ ] Path parser (group/artifact/version/file)
- [ ] Download → scan → serve pipeline (reuse pattern z PyPI)
- [ ] Cache hit path
- [ ] Metadata/checksum pass-through
- [ ] Config (port + upstream)
- [ ] main.go integrace
- [ ] Unit testy
- [ ] Docs
- [ ] Path traversal ochrana (path.Clean, reject ..)
- [ ] Classifier support v path parseru
- [ ] .asc signature file pass-through
- [ ] Bezpečná upstream URL construction (url.JoinPath)

## Rizika a mitigace

| Riziko | Dopad | Pravděpodobnost | Mitigace |
|--------|-------|-----------------|----------|
| Komplexní Maven path parsing | Střední — edge cases | Střední | Extensive unit testy s reálnými Maven paths |
| JAR obfuscation (bytecode) | Nízký — scanner limitace | Střední | Trivy + OSV pokryjí známé CVE; bytecode analysis budoucí |
| Path traversal | Vysoký — přístup mimo namespace | Střední | path.Clean(), reject `..`, validate components |
| POM s malicious repositories | Střední — supply chain | Střední | Dokumentovat jako known limitation; budoucí POM validation |
| SSRF přes upstream URL construction | Střední — internal access | Nízká | url.JoinPath(), nikdy string concatenation |
| Snapshot verze (1.0-SNAPSHOT) | Nízký — jiná resolution sémantika | Střední | Dokumentovat jako known limitation pro v1.1 |

## Testování

- `TestMavenAdapter_ParsePath_GroupArtifactVersion` — path parsing
- `TestMavenAdapter_DownloadJAR_ScanAndServe` — full pipeline
- `TestMavenAdapter_MetadataXML_PassThrough` — metadata bez scanu
- `TestMavenAdapter_CacheHit_ServesFromCache` — cache path
- `TestMavenAdapter_MaliciousJAR_Returns403` — block flow

## Poznámky

- **Maven snapshot verze:** `1.0-SNAPSHOT` verze mají jinou resolution sémantiku (timestamped builds). Pro v1.1 nejsou explicitně podporovány — budou proxovány ale cache behavior může být neočekávaný.
- **Dokumentační deliverables:** `docs/index.md`, `docs/adapters.md`, `docs/configuration.md`.

## Reference

- `docs/initial-analyse.md:591-594` — Maven adapter spec
- `internal/adapter/pypi/pypi.go` — reference adapter pattern
- `internal/scanner/interface.go:10-15` — Ecosystem constants (EcosystemMaven nutno přidat)
